
/**
 * A timestamp represented as the number of milliseconds since the Unix epoch (January 1, 1970).
 */
export type UnixTimestamp = number;

/**
 * An array of Unix timestamps in milliseconds, representing time-series data.
 */
export type UnixTimestamps = UnixTimestamp[];
